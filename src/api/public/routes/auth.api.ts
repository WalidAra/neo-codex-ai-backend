import { Router } from "express";
const router = Router();

router.get("/refresh", () => {});
router.post("/signin", () => {});
router.post("/signup", () => {});
router.get("/google", () => {});
router.get("/google/callback", () => {});

export default router;
